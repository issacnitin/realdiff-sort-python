name: RealDiff

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: issacnitin/RealDiff@9c9a7fad069a122afce6a3410451b9fd01e53ed3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          gate: warn-only
          post: 'true'
          strict: 'true'